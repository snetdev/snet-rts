module mini;

use Structures: all;
use StdIO: all;


export all;


void A( SNet::SNet &hnd, int[.] A)
{
  B = A + 1;
  C = B + 1;
  
  SNet::out( hnd, 1, sacprelude::wrap( B));
  SNet::out( hnd, 1, sacprelude::wrap( C));
}
