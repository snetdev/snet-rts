module mini;

use Structures: all;
export all;


void A( SNet::SNet &hnd, int[.] A)
{
  B = A + 1;
  C = B + 1;
  
  SNet::outRaw( hnd, 1, sacprelude::wrap( B));
  SNet::outRaw( hnd, 1, sacprelude::wrap( C));
}
