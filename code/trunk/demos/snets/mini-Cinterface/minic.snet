<?xml version="1.0"?>
<metadata xmlns="snet-home.org" >

  <!-- C4SNet is the default interface -->
  <interface name="C4SNet" default="true" />

</metadata>
 


net minic 
{
  box dec((A) -> (B));
  box init(-> (X));
} connect dec | init;

