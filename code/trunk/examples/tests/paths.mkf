

INCLUDES  = -I.

ifdef SNET_INCLUDES
INCLUDES += -I$(SNET_INCLUDES)
endif

# following should go at some point in time
INCLUDES += -I$(SNETBASE)/include

LIBRARIES = $(SOCKETLIBS)
SNETCFLAGS+= $(SNETTESTFLAGS)
